[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "onneggs"
version = "0.0.4"
requires-python = ">=3.10"
dependencies = ["onnx", "maturin", "numpy", "onnxruntime", "click", "eggwrap"]
readme = "readme.md"