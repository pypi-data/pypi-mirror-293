[tool.poetry]
name = "zdppy-mysql"
version = "0.1.5"
description = "Python连接MySQL的底层核心库"
authors = ["张大鹏 <1156956636@qq.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
