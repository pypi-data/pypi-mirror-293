from setuptools import setup, find_packages

setup(
    name='gzintrospect',
    version='0.1.17',
    author='shenzhouxu',
    packages=find_packages(),
    install_requires=[
        # 依赖列表
    ],
    # 其他元数据
)
