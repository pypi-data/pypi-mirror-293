[tool.poetry]
name = "pg-rls-sqlalchemy"
version = "0.1.0"
description = ""
authors = ["Matthew Beatty <beattyml1@gmail.com>"]
readme = "README.md"
include = [{path="README.md"}, {path="LICENSE"}]
packages = [
    { include = "pg_rls", from = "." },
]

[tool.poetry.dependencies]
python = "^3.11"
sqlalchemy = "^2.0.32"
alembic = "^1.13.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
