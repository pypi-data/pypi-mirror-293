[tool.poetry]
name = "game-realm-keeper"
version = "0.4.0"
description = "A package to manage RPG resources."
authors = ["sebastian-hsu <sebastian.hsu@gmail.com>"]
readme = "README.md"
packages = [{include = "realm_keeper", from = "src"}]

[tool.poetry.dependencies]
python = "^3.11"
pydantic = "^2.8.2"
build = "^1.2.1"
twine = "^5.1.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
