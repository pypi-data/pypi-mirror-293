#import "../item/extendedFragment.graphql"

query ($id: ID!) {
  item: checkOutItemsById(id: $id) {
    item {
      ...ItemExtendedParts
    }
    containedItems {
      ...ItemExtendedParts
    }
  }
}
