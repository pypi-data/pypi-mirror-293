#import "./editFragment.graphql"

mutation categoryMutation($input: CheckOutProcessMutationInput!) {
  checkOutProcess(input: $input) {
    checkOutProcess {
      ...CheckOutProcessEditParts
    }
  }
}
