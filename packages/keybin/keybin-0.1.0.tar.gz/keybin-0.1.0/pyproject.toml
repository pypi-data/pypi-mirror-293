[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "keybin"
version = "0.1.0"
description = "A command-line interface for managing secrets in a secure vault."
authors = [{name = "Yeshwant Kuriyavar", email = "yeshwa916@gmail.com"}]
dependencies = ["requests", "pymongo", "pyperclip"]
readme = "README.md"

[project.scripts]
keybin = "main:main"
