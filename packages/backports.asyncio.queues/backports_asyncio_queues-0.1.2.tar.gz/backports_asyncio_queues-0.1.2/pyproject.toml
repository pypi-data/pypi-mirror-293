[project]
name = "backports.asyncio.queues"
version = "0.1.2"
description = "Backport python 3.13 asyncio Queue with shutdown."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backports/"]
