[tool.poetry]
name = "sqlpickle"
version = "0.1.2"
description = "Simple KV Storage and memoization based on sqlite"
authors = ["Slava Vishnyakov <bomboze@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
